server:
  port: 8082

spring:
  application:
    name: discount-engine-service

logging:
  level:
    com.loyalty.engine: INFO

admin:
  service:
    base-url: ${ADMIN_SERVICE_URL:http://localhost:8081}
